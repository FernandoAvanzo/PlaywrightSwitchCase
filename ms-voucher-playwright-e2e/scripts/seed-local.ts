/**
 * Ponto de extensão para seed local.
 *
 * A recomendação é priorizar criação de massa por API para preservar regras de domínio.
 * Quando for necessário inserir dados diretamente, adicione scripts SQL descartáveis em:
 *   docker/mysql/init/
 *   docker/oracle/init/
 */
console.log('Seed local: nenhum seed direto aplicado. Use as APIs ou adicione SQL descartável conforme necessidade.');
